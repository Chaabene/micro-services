spring.application.name=limits-service
server.port=8080
#le nom de fichier sur le serveur de configuration(git) , c'est optionnel , par defaut il prendera le nom de l'applicatipn(spring.application.name=limits-service)
spring.cloud.config.name=limits-service

#deux moyens pour définir le profile 
#spring.cloud.config.profile=dev
spring.profiles.active=qa

#url de server de configuraion
spring.cloud.config.uri=http://localhost:8888

management.endpoints.web.exposure.include=bus-refresh
